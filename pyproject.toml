[project]
name = "chronicle-hydra"
version = "0.1.0"
description = "Temporal, abstaining agent memory on HydraDB"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "httpx>=0.27", "pydantic>=2.0"]

[project.optional-dependencies]
llm = ["openai>=1.50"]
